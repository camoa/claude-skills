<!--
  Generated by /{plugin_name}:install-remembrance-hook on {generated_date}.
  Re-run that command if the project name or state path changes — this file
  is a static snapshot and does NOT track the plugin's source-of-truth state.
  You MAY freely edit the "User-specific reminders" section below by hand;
  re-running the install command preserves whatever you put there.
-->
# Session primer — {plugin_name}

This session is working on the **{project_name}** project, which uses the
**{plugin_name}** plugin.

- Plugin state: `{state_path}`
- {resume_hint}

## User-specific reminders

{user_additions}

# Glossary

Covers every word AIDA uses in exactly one sense, so the same thing is never called two names.
Short by design: a word is here because a check enforces it.

**Not written yet.** It is written when the controlled vocabulary is built. Until then this page is a placeholder, and
nothing in it is a promise.

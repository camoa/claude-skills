<!--
  Generated by /ai-dev-assistant:install-remembrance-hook on {generated_date}.
  Re-run that command if the project name, memory path, or code path changes —
  this file is a static snapshot and does NOT track project_state.md.
  You MAY freely edit the "User-specific reminders" section below by hand;
  re-running the install command preserves whatever you put there.
-->
# Session primer — ai-dev-assistant

This session is working on the **{project_name}** project. It uses the
**ai-dev-assistant** development framework (Research → Architecture →
Implementation phases, with SOLID / TDD / DRY / security gates).

- Framework memory: `{memory_path}`
- Code path: `{code_path}`
- Run `/ai-dev-assistant:next` to see the current phase and task.
- If a task has compiled work-orders, check `<task>/work-orders/` — `/ai-dev-assistant:next` surfaces their status and `/run-work-orders` builds them.
- Before ending your work, run `/ai-dev-assistant:save-session` to persist
  in-flight state.

## User-specific reminders

{user_additions}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AIDA implementation ledger",
  "description": "The one store for what an implementation run has done, at <task folder>/implementation/ledger.json (ideal/implementation.md, 'The ledger is the state file, not a status log'). It exists so a person who was not watching can arrive afterwards and decide what to redo, and so a run that died mid-order knows where it got to. Plain JSON, no fences, no markdown. Nothing derivable is stored: which orders depend on which lives in the work orders, and which order is in flight is the one order with a step reached, no halt reason and no close, since orders run one at a time. A field present in a real ledger but absent from this schema's `properties` is a defect in the schema, not a value to silently accept, which is why `additionalProperties` is false. This schema holds what exists, never what is planned. The commit range an order produced arrived on 2026-09-11 with `close`, the step that writes it. The checkpoint's own judgements, and the person's grant of another attempt, arrived the same day with `tests-freeze` and `grant-attempt`. foundations.md: a new thing gets the least that works, and a field nobody has asked for is a field nobody can cut back.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "startedFrom",
    "startedAt",
    "runMode",
    "snapshotHash",
    "orders",
    "criteria"
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "The shape this file was written in, the same field every other AIDA record carries."
    },
    "startedFrom": {
      "type": "string",
      "pattern": "^[0-9a-f]{7,40}$",
      "description": "The commit the build started from, captured by the first step before anything is written. Rollback is a reset to it, which is what replaces the isolated working copy version 6 decided against. Version 5 captures nothing of the kind and its own ledger needs one. Rewritten by one thing only, `start --rebased-onto <commit>`, after a rebase or an amend left this commit on no branch; the value it replaced is kept under startedFromBefore."
    },
    "startedAt": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
      "description": "The UTC time the ledger was opened, set by the first step and kept by every reopening. The one clock the run's `budget.minutes` ceiling in task.json is measured from: what was spent is recomputed from this and the wall clock at each dispatch, and stored nowhere, so nothing a builder writes can reset it."
    },
    "runMode": {
      "type": "string",
      "enum": [
        "interactive",
        "autonomous"
      ],
      "description": "The mode this build runs under: the task's own mode for the implement stage, read through task_run_mode and written by every `start`, new or resumed. The steps between two starts read it here and never from task.json, so one run applies one rule from its first dispatch to its last record. A person who changes the task's mode runs `start` again, which is where a resumed run begins anyway, and the change takes from there. Review reads the task, not this copy (nyc defect 20)."
    },
    "snapshotHash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "The `hash` of the snapshot this ledger was opened against. Reopening compares it to the snapshot on disk and refuses when they disagree, so a ledger and a snapshot that do not belong together are never read as a pair. The failure this prevents is a resumed run recording progress against orders it was not built from."
    },
    "orders": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/orderEntry"
      },
      "description": "One entry per work order in the snapshot, opened by the first step at no step completed. Order-level state is the thing version 5 records nowhere, which is why a person arriving at one of its runs cannot list which orders finished."
    },
    "criteria": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/criterionEntry"
      },
      "description": "One entry per criterion in the snapshot's contract, opened by the first step as not judged. This holds whether the tests claiming to prove a criterion were confirmed to do so, never the criterion itself, which lives in the snapshot."
    },
    "startedFromBefore": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/startedFromBefore"
      },
      "description": "One entry per value startedFrom held before `start --rebased-onto` rewrote it, in the order it happened. Present only when at least one rewrite was. A resumed `start` refuses (exit 82) when HEAD does not descend from startedFrom, because a rebase or an amend left that commit on no branch: `preconditions` would label the baseline with it and `finish` would compute a range git cannot resolve. The rewrite keeps the old value here rather than dropping it, so a reader can tell which commit the baseline on disk was taken at, and why it must be retaken (nyc defect 19)."
    },
    "haltsCleared": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/haltCleared"
      },
      "description": "One entry per halt cleared, in the order it happened. Present only when at least one was. Three actions write here. `clear-halt` writes one when a person did what the halt named. `retake-tests` writes one when a test-wrong ruling sent the order back to its tests. A resumed `start` writes one when its own comparison no longer finds the drift a halt was written for, since `start` is the one action that computes drift (live-run row 143). The grant and the restart clear their own halts and record them in their own fields. The order's haltedBecause is removed at the same moment, so the reason lives on here and nowhere else. A `start` clearing removes only the `design drift` segment, so the order stays halted for any other segment it carried."
    },
    "resnapshots": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/resnapshot"
      },
      "description": "One entry per order the snapshot took a live copy of after the first capture, in the order it happened. Present only when at least one was. `start` writes one for a drifted order that had not started, and `restart` for each order it resets, both after design closed on the live copy (live-run row 72). An order is frozen when it starts, not when the stage starts, and this is the record of which shape each order was built from."
    }
  },
  "$defs": {
    "orderEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "lastStep",
        "attemptsUsed",
        "roundsUsed"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^wo[1-9][0-9]*$",
          "description": "The work order's own id, minted by design. Joined on, never re-minted."
        },
        "lastStep": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            null,
            "tests-frozen",
            "code-written",
            "checks-passed",
            "reviewed",
            "fixed",
            "closed"
          ],
          "description": "The last step of this order that finished, or null when none has. A resumed run redoes the step after this one, which is why every step must be safe to run again: the record names what completed, never what was in progress. The order's state derives from this and needs no field of its own. Null is not started, `closed` is done, anything between with a halt reason is halted and without one is in flight."
        },
        "haltedBecause": {
          "type": "string",
          "minLength": 1,
          "description": "Why this order stopped, in plain words, present only when it is halted. Absent means the order is not halted; it never holds an empty string, because absent and empty would be one value for two facts."
        },
        "attemptsUsed": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "How many times a builder has been given this order. The loop stops on a counter and never on a reviewer being satisfied, so a counter that resets when a run is resumed is not a cap. Version 5 keeps the same count in a crash-atomic sidecar whose header warns never to reset it."
        },
        "roundsUsed": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "How many fix rounds this order has had. One round is one fixer and one verification. Same reason as attemptsUsed."
        },
        "commitRange": {
          "type": "string",
          "pattern": "^[0-9a-f]{7,40}\\.\\.[0-9a-f]{7,40}$",
          "description": "The commits this order produced, the first commit of its own build attempt and the code repository's HEAD when it closed, joined by two dots. For an order whose proof is record both commits are the project folder's, where its deliverable landed (nyc defect 17). Written by `close` and by nothing else, so its presence is a second reading of a closed order beside lastStep. The left side is the build record's own startedAt for this order rather than the ledger's startedFrom: orders build one after another, so the ledger's value would sweep every earlier order's commits into this one's range, and a range that names work the order did not do is worse than no range at all."
        },
        "attemptsAllowed": {
          "type": "integer",
          "minimum": 1,
          "description": "How many attempts this order is allowed, present only when a person has granted one beyond the default. Absent means the script's own BUILD_ATTEMPTS_ALLOWED. `grant-attempt` writes it, one higher than it was, and never lowers it. The grant raises the allowance rather than lowering attemptsUsed, so the count of what a builder already spent stays a fact about the past (ideal/implementation.md, 'What a halt at the attempt cap leads to')."
        },
        "grants": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/grant"
          },
          "description": "One entry per attempt a person has granted on this order, in the order they were granted. Present only when at least one was. A grant with no record of who asked for what is a cap nobody can audit afterwards."
        },
        "doneWhenJudgement": {
          "type": "object",
          "additionalProperties": false,
          "required": ["verdict", "judgedBy", "note"],
          "properties": {
            "verdict": { "$ref": "#/$defs/judgement/properties/verdict" },
            "judgedBy": { "$ref": "#/$defs/judgement/properties/judgedBy" },
            "note": { "$ref": "#/$defs/judgement/properties/note" }
          },
          "description": "The checkpoint's verdict on the tests that prove this order's own doneWhen rather than a criterion: the `--row` keyed by the order's id, written by `tests-freeze` and replaced on every freeze of this order. Present only when the order froze such a test. A supporting order serves criteria it cannot observe, so its tests are judged against its doneWhen text instead of a verify clause (live-run row 59). Counted with the criterion judgements in every rowsJudgedByModel, so a model's reading here is listed for a person to judge again like any other."
        },
        "absenceClauses": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "The doneWhen clauses of this order that assert an absence, each verbatim, written by `tests-freeze --absence` and replaced on every freeze of this order. Present only when the freeze routed at least one. Such a clause cannot be watched failing: the tree is already in the state it asserts, and making a test of it fail means adding the very thing the clause forbids (live-run row 184). It is answered at review instead, where the architecture reviewer judges it against the task's own diff. The list is what makes the route visible: a clause here is owed a verdict, and a clause nobody routed is owed a test."
        },
        "retakes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/retake"
          },
          "description": "One entry per time `retake-tests` sent this order back to tests-frozen after a person ruled a finding `test-wrong` at `verify-record`, in the order it happened. Present only when at least one was. The build, review, fix and verify records move aside, the step goes back, and attemptsUsed stays: the attempts were real, against the old test, and a spent counter is the grant's to answer. roundsUsed goes back to 0, because the review record it counted moved with the rest (live-run row 110)."
        }
      }
    },
    "criterionEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "rowState"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^c[1-9][0-9]*$",
          "description": "The criterion id scope minted. Joined on, never re-minted, and never the criterion's text."
        },
        "rowState": {
          "type": "string",
          "enum": [
            "not-judged",
            "confirmed",
            "rejected"
          ],
          "description": "Whether the tests claiming to prove this criterion were confirmed to observe what its verify clause describes. Three values, because unconfirmed reading the same as confirmed is how nobody can tell what is still owed. Opened at not-judged by the first step."
        },
        "judgements": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/judgement"
          },
          "description": "One entry per order whose frozen tests name this criterion, appended by `tests-freeze` when that order's rows are frozen; a serving order whose tests name only its own done-when appends none, and its judgement sits on its ledger entry as doneWhenJudgement. Present only on a criterion a machine verifies: one a person verifies carries a checklist in the frozen test record and never a judgement, and completion confirms it. `close` reads this list to decide rowState."
        }
      }
    },
    "startedFromBefore": {
      "type": "object",
      "additionalProperties": false,
      "required": ["commit", "at"],
      "properties": {
        "commit": {
          "type": "string",
          "pattern": "^[0-9a-f]{7,40}$",
          "description": "The value startedFrom held before the rewrite."
        },
        "at": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "The UTC date of the rewrite, YYYY-MM-DD. Set by the script, never typed."
        }
      }
    },
    "resnapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "from", "to", "at"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^wo[1-9][0-9]*$",
          "description": "The order whose live copy the snapshot took."
        },
        "from": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "The snapshot's hash before the copy was taken."
        },
        "to": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "The snapshot's hash after, which snapshotHash also became."
        },
        "at": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "The UTC date the copy was taken, YYYY-MM-DD. Set by the script, never typed."
        }
      }
    },
    "retake": {
      "type": "object",
      "additionalProperties": false,
      "required": ["finding", "at", "movedTo", "freezeCommit"],
      "properties": {
        "finding": {
          "type": "string",
          "pattern": "^f[1-9][0-9]*$",
          "description": "The finding ruled test-wrong, read from the front of the halt reason `verify-record` wrote."
        },
        "at": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "The UTC date of the retake, YYYY-MM-DD. Set by the script, never typed."
        },
        "movedTo": {
          "type": "string",
          "minLength": 1,
          "description": "The folder the records moved to, implementation/retaken-<order>-<n>/, n counting from 1. Nothing is deleted."
        },
        "freezeCommit": {
          "type": "string",
          "pattern": "^[0-9a-f]{7,40}$",
          "description": "The commit the frozen test record held at the retake. HEAD is past it by then, so `tests-freeze` reads this to let the one freeze after the retake through its once-per-commit refusal (exit 35) and overwrite the record with retakenFrom."
        }
      }
    },
    "haltCleared": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "reason", "clearedAt", "because"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^wo[1-9][0-9]*$",
          "description": "The order whose halt was cleared."
        },
        "reason": {
          "type": "string",
          "minLength": 1,
          "description": "The haltedBecause text as it stood, every segment, copied before it was removed. A clearing that removes one segment copies the whole text here too, so the entry says what the order was stopped for."
        },
        "clearedAt": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "The UTC date the halt was cleared, YYYY-MM-DD. Set by the script, never typed."
        },
        "because": {
          "type": "string",
          "minLength": 1,
          "description": "What the person did about the halt, in their own words. Required, because a halt cleared with no reason reads as the stop never having meant anything."
        }
      }
    },
    "grant": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "reason",
        "grantedAt",
        "allowedAfter"
      ],
      "properties": {
        "reason": {
          "type": "string",
          "minLength": 1,
          "description": "Why the person granted another attempt, in their own words. Required, because a grant with no reason reads as the cap never having meant anything."
        },
        "grantedAt": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "The UTC date the grant was made, YYYY-MM-DD. Set by the script, never typed."
        },
        "allowedAfter": {
          "type": "integer",
          "minimum": 1,
          "description": "What attemptsAllowed became with this grant. Recorded per grant so a reader sees the sequence rather than only the total."
        }
      }
    },
    "judgement": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "unit",
        "verdict",
        "judgedBy",
        "note"
      ],
      "properties": {
        "unit": {
          "type": "string",
          "pattern": "^wo[1-9][0-9]*$",
          "description": "The work order whose rows this judgement answers. One order judges one criterion once: a second freeze of the same order replaces its entry rather than adding one, so a row is never counted twice."
        },
        "verdict": {
          "type": "string",
          "enum": [
            "confirmed",
            "rejected"
          ],
          "description": "Whether these tests observe the part of the verify clause this order is responsible for. A rejected row never reaches this file today, because `tests-freeze` refuses the freeze on one and sends the row back to the test author; the value exists so `close` can derive a rejection rather than assume it away."
        },
        "judgedBy": {
          "type": "string",
          "enum": [
            "person",
            "model",
            "gate"
          ],
          "description": "Who answered. `model` marks a row the row-checker judged, a context that did not write the tests, and that no person re-read. The checker judges every row in both modes against the test-authoring recipe. On an attended run a person answers only a row it rejected, and that answer is `person` (live-run row 70). `person` is refused on an autonomous run. A model's row is weaker evidence than a person's reading, and the record says so rather than marking the row passed. A person returning can then list exactly those rows and judge them again (ideal/implementation.md, 'This departs from the tenth invariant, and says so'). `gate` marks a criterion owned by an order whose proof is gate, written by `close` when the order's configuration-gate check was met on its closing attempt: the recipe's own lines decided it, deterministically. A third value rather than `model`, because a model's row is weaker evidence queued for a person to judge again and this one is not; rather than `person`, because nobody read anything. rowsJudgedByModel never counts it (live-run row 65). A criterion owned by an order whose proof is record is written by `close` from the order's done-when judgement with that row's own judge, `person` or `model`, never `gate`: somebody read the row (nyc defect 17)."
        },
        "note": {
          "type": "string",
          "minLength": 1,
          "description": "What the judge saw, in their own words. Required with every verdict, because a verdict with nothing to read beside it cannot be re-judged."
        }
      }
    }
  }
}

# Design

Covers turning research into a specification: where logic lives, what gets built against what
already exists, and which of your stack's patterns each piece follows. Covers how the design is
checked against the scope contract before any code is written.

**Not written yet.** It is written when the design stage is built. Until then this page is a placeholder, and
nothing in it is a promise.
